# Dependencias del backend de AutoPrime (cuarto avance).
# Versiones fijas para que la instalacion sea reproducible.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Base de datos: SQLAlchemy sincrono sobre MySQL/MariaDB (XAMPP).
SQLAlchemy==2.0.52
PyMySQL==1.2.0
# Driver asincrono para la API. PyMySQL se queda para los scripts de
# linea de ordenes, que van en sincrono a proposito.
aiomysql==0.3.2

# Seguridad.
# Se usa bcrypt directamente en lugar de passlib: passlib no recibe
# mantenimiento y rompe con bcrypt 4.x al leer su atributo __about__.
# Los hashes son los mismos, asi que los usuarios creados por el backend
# anterior en Node siguen pudiendo iniciar sesion.
bcrypt==5.0.0
python-jose[cryptography]==3.5.0

# Lectura del .env (pydantic-settings lo usa por debajo).
python-dotenv==1.2.3

# Formularios del login OAuth2 que usa la documentacion de Swagger.
python-multipart==0.0.32

# --- Quinto avance ---

# Reportes. reportlab y XlsxWriter son Python puro: no arrastran librerias
# del sistema, que es lo que descarta a WeasyPrint aqui. El contenedor de
# Render no trae ni Chrome ni cairo, asi que un generador que dependiera de
# ellos funcionaria en el portatil y fallaria en produccion.
reportlab==5.0.1
XlsxWriter==3.2.9

# Cliente HTTP asincrono: habla con Groq y con el calendario de festivos sin
# bloquear el bucle de eventos, que es justo lo que se gano al pasar a async.
httpx==0.28.1

# Base de datos de zonas horarias.
#
# En Windows no existe ninguna y `zoneinfo` se queda sin zonas; en Linux
# suele venir con el sistema, pero "suele" no basta para lo que decide a
# que dia pertenece una venta. Declararla cuesta un paquete y quita la duda
# en los dos sitios. Ver app/core/tiempo.py.
tzdata==2026.4

# Pruebas.
pytest==9.1.1
pytest-asyncio==1.4.0
